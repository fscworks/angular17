import { Component } from '@angular/core';

@Component({
  selector: 'app-if-pg',
  standalone: true,
  imports: [],
  templateUrl: './if-pg.component.html',
  styleUrl: './if-pg.component.scss',
})
export class IfPgComponent {
  showText = true;
}
